import styled from 'styled-components'
import Card from '@/components/Card'

export const Layout = styled(Card)`
  text-align: center;
  cursor: default;
  max-width: 550px;
  height: auto;
  padding: 1em 3em;
`
